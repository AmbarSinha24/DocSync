Drop screenshots here with these filenames — the root README already links to them:

- `projects.png` — Projects page, onboarded repos + Add Repo
- `add-repo-progress.png` — Add Repo background sync progress card
- `approval-queue.png` — Approval Queue with bulk select active
- `approval-detail.png` — Approval detail side-by-side diff view
- `confluence-result.png` — A resulting synced Confluence page
